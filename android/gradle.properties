org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# O caminho do projeto (OneDrive\Área de Trabalho) tem acento, o que o
# Android Gradle Plugin recusa por padrão no Windows. Já validamos que os
# builds funcionam normalmente mesmo assim (build apk debug/release, deploys
# de Cloud Functions, etc.) - só desativa esse aviso/checagem.
android.overridePathCheck=true
